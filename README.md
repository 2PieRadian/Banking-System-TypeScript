`npm init -y`

`npm install typescript @type/node --save-dev`

`npx tsc --init`

Making changes in `tsconfig.json`
`"outDir": "./dist"`
`"target": "es6"`

Build Project
`npx tsc`

Run Project
`npm start`
